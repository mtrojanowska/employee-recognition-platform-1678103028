<h1>KUDOS#CREATE</h1> 
<%= form_with model: @kudo, local: true do |f| %>
  <% if @kudo.errors.any? %>
    <%= pluralize(@kudo.errors.count, "error") %> this kudo from being saved. 
    <% @kudo.errors.full_messages.each do |msg| %>
      <%= msg %>      
    <% end %>
  <% end %><br>

<%= f.hidden_field :giver_id, value: params[:giver_id]%>

<%= f.label :title %><br>
<%= f.text_field :title %><br>

<%= f.label :say_something_nice %><br>
<%= f.text_area :content %><br>

<%= f.label :to %><br>
<%= f.collection_select(:receiver_id, Employee.order(:email), :id, :email, prompt: 'Choose the coworker') %><br>

<%= f.submit %>
<% end %>
<%# .where.not(id: current_employee.id) %>